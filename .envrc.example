# Cloudflare API Token (Required)
export CLOUDFLARE_API_TOKEN=""

# Color theme (Optional)
# Options: ansi, mocha, nord, dracula, rose-pine, tokyo-night, gruvbox, everforest
export CFTUI_THEME="ansi"

# Log file path (Optional)
# export CFTUI_LOG="$HOME/.local/state/cftui/cftui.log"

# Debug mode (Optional)
export CFTUI_DEBUG="false"
